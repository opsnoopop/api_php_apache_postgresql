# /var/www/html/.htaccess
RewriteEngine On

# ถ้าเป็นไฟล์/ไดเรกทอรีจริง ให้เสิร์ฟตรง ๆ
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# นอกนั้นโยนเข้า index.php
RewriteRule ^ index.php [QSA,L]
